import { FrutasOfertaPipe } from './frutas-oferta.pipe';

describe('FrutasOfertaPipe', () => {
  it('create an instance', () => {
    const pipe = new FrutasOfertaPipe();
    expect(pipe).toBeTruthy();
  });
});
